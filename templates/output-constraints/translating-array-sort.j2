* When translating `sort()` method calls on arrays to JavaScript, provide exactly two translations:
  i. Using the default `sort()` method (e.g. `arr.sort();`)
  ii. Using a custom comparator function for numerical sorting (e.g. `arr.sort((a, b) => a - b);`)