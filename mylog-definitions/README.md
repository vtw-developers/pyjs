# About
Definitions of functions that are used in test scripts during
translation rule validation and rule application phase.

These snippets of code are inserted into `tar_program` to log input and output to the program under test.
- `mylog.*` is for `src_program`
- `mylog_match.*` is for `tar_program`
- Those starting with underscore are not used in original DuoGlot work.