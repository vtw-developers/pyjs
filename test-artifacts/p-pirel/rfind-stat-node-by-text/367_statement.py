while i < m and j < n:
    myexactlog(5, 0)
    if arr1[i] < arr2[j]:
        myexactlog(6, 0)
        sorted1[d] = arr1[i]
        myexactlog(7, sorted1)
        i += 1
        myexactlog(8, i)
    else:
        myexactlog(9, 0)
        sorted1[d] = arr2[j]
        myexactlog(10, sorted1)
        j += 1
        myexactlog(11, j)
    d += 1
    myexactlog(12, d)
    break