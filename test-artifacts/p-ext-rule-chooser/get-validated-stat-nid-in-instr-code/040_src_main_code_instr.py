def f_gold(str_0):
    zeros = 0
    myexactlog(1, zeros)
    ones = 0
    myexactlog(2, ones)
    for i in range(0, len(str_0)):
        myexactlog(3, 0)
        ch = str_0[i]
        myexactlog(4, ch)
        if ch == "0":
            myexactlog(5, 0)
            pass
        else:
            myexactlog(6, 0)
            pass
        break