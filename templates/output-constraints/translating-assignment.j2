* When translating assignment statements to JavaScript, provide exactly two translations:
  i. Variable declaration with `var` (e.g. `var a = 1;`)
  ii. Assignment expression without `var` (e.g. `a = 1;`)