{
  "template_id": 0,
  "src_lang": "py",
  "tar_lang": "js",
  "template_origin": "midVal1 = nums1[i + k // 2 - 1] if i + k // 2 - 1 < m else float('inf')",
  "context_node_type": "expression_statement",
  "context_node_id": 1,
  "problematic_node_type": "expression_statement",
  "problematic_node_id": 1,
  "problematic_node_path": [],
  "is_valid_template": true,
  "is_insert_secret_fn": false,
  "contexts": [
    {
      "source_context": [
        [
          "py.expression_statement"
        ]
      ],
      "target_context": [
        [
          "unknown"
        ]
      ]
    }
  ],
  "template_origin_before_simplification": "midVal1 = nums1[i + k // 2 - 1] if i + k // 2 - 1 < m else float('inf')",
  "partial_program": "pirel_replace_var;",
  "src_program": "\nimport math\nfrom math import inf\nfrom typing import *\ndef f_gold(nums1: List[int], nums2: List[int]) -> float:\n    def findKth(i, j, k):\n        if i >= m:\n            return nums2[j + k - 1]\n        if j >= n:\n            return nums1[i + k - 1]\n        if k == 1:\n            return min(nums1[i], nums2[j])\n        midVal1 = nums1[i + k // 2 - 1] if i + k // 2 - 1 < m else float('inf')\n        midVal2 = nums2[j + k // 2 - 1] if j + k // 2 - 1 < n else float('inf')\n        if midVal1 < midVal2:\n            return findKth(i + k // 2, j, k - k // 2)\n        return findKth(i, j + k // 2, k - k // 2)\n    m, n = len(nums1), len(nums2)\n    left, right = (m + n + 1) // 2, (m + n + 2) // 2\n    return (findKth(0, 0, left) + findKth(0, 0, right)) / 2\n",
  "pre_context": "if i >= m:\n    return nums2[j + k - 1]\nif j >= n:\n    return nums1[i + k - 1]\nif k == 1:\n    return min(nums1[i], nums2[j])\npirel_pre_ctx_spec_identifier",
  "tsps": [
    [
      "id_kwxv = id_ufkx",
      "id_buwx = 5501"
    ],
    [
      "id_ycg = id_roi if id_dpem else id_pfy",
      "id_tupg = 8798 if 8096 else 2655"
    ],
    [
      "id_nkpb = id_drqp[id_ofyi] if id_ljo < id_sdl else float('inf')",
      "id_rixu = id_szpj[8314] if 7252 < id_zwk else float('inf')"
    ],
    [
      "id_mao = id_qtkd[id_anw - 837] if id_rbsa < id_ealw else float('inf')",
      "id_dig = id_unqd[1266 - id_pgi] if 12 < id_tuw else float('inf')"
    ],
    [
      "id_yuw = id_bvxl[id_odrx] if id_rglo - 2065 < id_zqi else float('inf')",
      "id_kmfs = id_bmt[6963] if 4392 - id_qszi < id_cuo else float('inf')"
    ],
    [
      "id_ikh = id_xln[id_zsm - 9146] if id_anbq - 6497 < id_jwxu else float('inf')",
      "id_ngw = id_qby[5745 - id_rgns] if 8515 - id_lvyi < id_cvy else float('inf')"
    ]
  ]
}
