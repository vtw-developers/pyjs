def f_gold(num):
    l = len(num)
    myexactlog(1, l)
    count_zero = 0
    myexactlog(2, count_zero)
    i = 1
    myexactlog(3, i)
    while i < l:
        myexactlog(4, 0)
        pass
        break