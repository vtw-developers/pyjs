if count_neg & 1:
    myexactlog(17, 5)
    if count_neg == 1 and count_zero > 0 and count_zero + count_neg == n:
        myexactlog(18, 4)
        myexactlog(19, 0)
        return 0
    prod = int(prod / max_neg)
    myexactlog(20, prod)