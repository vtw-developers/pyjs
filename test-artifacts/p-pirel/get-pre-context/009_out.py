if k >= n - 1:
    myexactlog(1, 0)
    myexactlog(2, n)
    return n
best = 0
myexactlog(3, best)
times = 0
myexactlog(4, times)
for i in range(n):
    myexactlog(5, 0)
    if a[i] > best:
        myexactlog(6, 2)
        pirel_pre_ctx_spec_identifier
    else:
        pass