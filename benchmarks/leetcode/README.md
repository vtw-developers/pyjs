# `py` directory
- contains the actual benchmark subjects
- 1067 subjects
- same as in DuoGlot

# `pysep` directory
- subjects from `py` are split into two files
  - `f_gold` - actual function that is being tested
  - test function
- removed `cov.py`, `cov.report` files; remaining files are the same as in DuoGlot