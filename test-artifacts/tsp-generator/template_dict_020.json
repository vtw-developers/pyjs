{
  "template_id": 0,
  "src_lang": "py",
  "tar_lang": "js",
  "template_origin": "if i >= m:\n    return",
  "context_node_type": "if_statement",
  "context_node_id": 1,
  "problematic_node_type": "if_statement",
  "problematic_node_id": 1,
  "problematic_node_path": [],
  "is_valid_template": true,
  "is_insert_secret_fn": true,
  "contexts": [
    {
      "source_context": [
        [
          "py.if_statement"
        ]
      ],
      "target_context": [
        [
          "unknown"
        ]
      ]
    }
  ],
  "template_origin_before_simplification": "if i >= m:\n    return nums2[j + k - 1]",
  "partial_program": "pirel_replace_var;",
  "src_program": "\nimport math\nfrom math import inf\nfrom typing import *\ndef f_gold(nums1: List[int], nums2: List[int]) -> float:\n    def findKth(i, j, k):\n        if i >= m:\n            return nums2[j + k - 1]\n        if j >= n:\n            return nums1[i + k - 1]\n        if k == 1:\n            return min(nums1[i], nums2[j])\n        midVal1 = nums1[i + k // 2 - 1] if i + k // 2 - 1 < m else float('inf')\n        midVal2 = nums2[j + k // 2 - 1] if j + k // 2 - 1 < n else float('inf')\n        if midVal1 < midVal2:\n            return findKth(i + k // 2, j, k - k // 2)\n        return findKth(i, j + k // 2, k - k // 2)\n    m, n = len(nums1), len(nums2)\n    left, right = (m + n + 1) // 2, (m + n + 2) // 2\n    return (findKth(0, 0, left) + findKth(0, 0, right)) / 2\n",
  "pre_context": "pirel_pre_ctx_spec_identifier",
  "tsps": [
    [
      "if id_fqn:\n    return",
      "if 8205:\n    return",
      "if id_myb:\n    return"
    ],
    [
      "if id_gbeq >= id_hrbi:\n    secret_fun_4071()",
      "if id_wle >= id_dlr:\n    secret_fun_4071()",
      "if id_fuzn >= id_ksdg:\n    secret_fun_4071()"
    ]
  ]
}
